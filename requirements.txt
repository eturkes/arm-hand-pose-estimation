openvino>=2023.1.0
opencv-python-headless
numpy
scipy
pygame-ce
tqdm
requests

# RTMW prototype (Phase 0) — install when ready to test:
# pip install rtmlib
# rtmlib pulls in onnxruntime and opencv-contrib-python;
# for OpenVINO backend, openvino (above) is already present.
